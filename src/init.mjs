// `folio init <dossier>` — crée un nouveau document à partir de template/.
//
// ÉTAPE 5 DU PLAN — VOLONTAIREMENT LA DERNIÈRE. Non implémenté.
//
// On ne peut pas générer un bon squelette tant qu'on ne sait pas ce qu'est un bon
// squelette : l'écrire tôt, c'est le réécrire trois fois. template/index.html reste
// donc une ébauche jusqu'à ce que les deux documents de recette soient portés.
//
// Sa raison d'être (même critère que <landscape-plate>, décision #9) : le squelette
// correct n'est pas devinable, et une erreur dedans donne une page blanche SANS message.

export async function init(_options) {
  throw new Error("init : pas encore implémenté — étape 5 du plan (docs/DECISIONS.md §6)")
}
